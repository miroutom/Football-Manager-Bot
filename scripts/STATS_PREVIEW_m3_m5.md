# Превью: стата по 15 матчам (м3–м5)
**Запись в БД не выполнялась** — только проверка.

## 1. м3 · Атлетик 1:1 Лейпциг
- Страны: Испания — Германия
- Тип: **ЛЧ** → БД `champions_league.db (ЛЧ)`
- Журнал `match_results.json`: ✓ в журнале
- `tournament` для статы: `cl`
- Строки игроков (голы/передачи): **не заданы в запросе** — нужны из скринов «Статистика игроков»
```
# пример формата (пока пусто):
# Имя 2 1
```

## 2. м3 · Атлетик 0:2 Дортмунд
- Страны: Испания — Германия
- Тип: **ЛЧ** → БД `champions_league.db (ЛЧ)`
- Журнал `match_results.json`: ✓ в журнале
- `tournament` для статы: `cl`
- Строки игроков (голы/передачи): **не заданы в запросе** — нужны из скринов «Статистика игроков»
```
# пример формата (пока пусто):
# Имя 2 1
```

## 3. м3 · Реал 3:2 Франкфурт
- Страны: Испания — Германия
- Тип: **ЛЧ** → БД `champions_league.db (ЛЧ)`
- Журнал `match_results.json`: ✓ в журнале
- `tournament` для статы: `cl`
- Строки игроков (голы/передачи): **не заданы в запросе** — нужны из скринов «Статистика игроков»
```
# пример формата (пока пусто):
# Имя 2 1
```

## 4. м4 · Дортмунд 1:2 Сити
- Страны: Германия — Англия
- Тип: **ЛЧ** → БД `champions_league.db (ЛЧ)`
- Журнал `match_results.json`: ✓ в журнале
- `tournament` для статы: `cl`
- Строки игроков (голы/передачи): **не заданы в запросе** — нужны из скринов «Статистика игроков»
```
# пример формата (пока пусто):
# Имя 2 1
```

## 5. м4 · Интер 0:3 Барселона
- Страны: Италия — Испания
- Тип: **ЛЧ** → БД `champions_league.db (ЛЧ)`
- Журнал `match_results.json`: ✓ в журнале
- `tournament` для статы: `cl`
- Строки игроков (голы/передачи): **не заданы в запросе** — нужны из скринов «Статистика игроков»
```
# пример формата (пока пусто):
# Имя 2 1
```

## 6. м4 · Атлетик 2:4 Динамо
- Страны: Испания — Россия
- Тип: **ЛЧ** → БД `champions_league.db (ЛЧ)`
- Журнал `match_results.json`: ✓ в журнале
- `tournament` для статы: `cl`
- Строки игроков (голы/передачи): **не заданы в запросе** — нужны из скринов «Статистика игроков»
```
# пример формата (пока пусто):
# Имя 2 1
```

## 7. м4 · Зенит 2:1 Севилья
- Страны: Россия — Испания
- Тип: **ЛЧ** → БД `champions_league.db (ЛЧ)`
- Журнал `match_results.json`: ✓ в журнале
- `tournament` для статы: `cl`
- Строки игроков (голы/передачи): **не заданы в запросе** — нужны из скринов «Статистика игроков»
```
# пример формата (пока пусто):
# Имя 2 1
```

## 8. м4 · Реал 1:1 Вольфсбург
- Страны: Испания — Германия
- Тип: **ЛЧ** → БД `champions_league.db (ЛЧ)`
- Журнал `match_results.json`: ✓ в журнале
- `tournament` для статы: `cl`
- Строки игроков (голы/передачи): **не заданы в запросе** — нужны из скринов «Статистика игроков»
```
# пример формата (пока пусто):
# Имя 2 1
```

## 9. м4 · Цска 2:4 Фиорентина
- Страны: Россия — Италия
- Тип: **ЛЧ** → БД `champions_league.db (ЛЧ)`
- Журнал `match_results.json`: ✓ в журнале
- `tournament` для статы: `cl`
- Строки игроков (голы/передачи): **не заданы в запросе** — нужны из скринов «Статистика игроков»
```
# пример формата (пока пусто):
# Имя 2 1
```

## 10. м4 · Наполи 1:3 Ливерпуль
- Страны: Италия — Англия
- Тип: **ЛЧ** → БД `champions_league.db (ЛЧ)`
- Журнал `match_results.json`: ✓ в журнале
- `tournament` для статы: `cl`
- Строки игроков (голы/передачи): **не заданы в запросе** — нужны из скринов «Статистика игроков»
```
# пример формата (пока пусто):
# Имя 2 1
```

## 11. м5 · Севилья 1:1 Лейпциг
- Страны: Испания — Германия
- Тип: **ЛЧ** → БД `champions_league.db (ЛЧ)`
- Журнал `match_results.json`: ✓ в журнале
- `tournament` для статы: `cl`
- Строки игроков (голы/передачи): **не заданы в запросе** — нужны из скринов «Статистика игроков»
```
# пример формата (пока пусто):
# Имя 2 1
```

## 12. м5 · Ювентус 1:0 Арсенал
- Страны: Италия — Англия
- Тип: **ЛЧ** → БД `champions_league.db (ЛЧ)`
- Журнал `match_results.json`: ✓ в журнале
- `tournament` для статы: `cl`
- Строки игроков (голы/передачи): **не заданы в запросе** — нужны из скринов «Статистика игроков»
```
# пример формата (пока пусто):
# Имя 2 1
```

## 13. м5 · Зенит 0:1 Дортмунд
- Страны: Россия — Германия
- Тип: **ЛЧ** → БД `champions_league.db (ЛЧ)`
- Журнал `match_results.json`: ✓ в журнале
- `tournament` для статы: `cl`
- Строки игроков (голы/передачи): **не заданы в запросе** — нужны из скринов «Статистика игроков»
```
# пример формата (пока пусто):
# Имя 2 1
```

## 14. м5 · Бавария 2:1 Мю
- Страны: Германия — Англия
- Тип: **ЛЧ** → БД `champions_league.db (ЛЧ)`
- Журнал `match_results.json`: ✓ в журнале
- `tournament` для статы: `cl`
- Строки игроков (голы/передачи): **не заданы в запросе** — нужны из скринов «Статистика игроков»
```
# пример формата (пока пусто):
# Имя 2 1
```

## 15. м5 · Атлетик 1:3 Сити
- Страны: Испания — Англия
- Тип: **ЛЧ** → БД `champions_league.db (ЛЧ)`
- Журнал `match_results.json`: ✓ в журнале
- `tournament` для статы: `cl`
- Строки игроков (голы/передачи): **не заданы в запросе** — нужны из скринов «Статистика игроков»
```
# пример формата (пока пусто):
# Имя 2 1
```

---
### Что будет сделано после подтверждения
1. Матчи уже в журнале — **повторно счёт не пишем** (иначе «уже сыгран»).
2. Для каждой строки игрока: `add_player_stats(..., tournament='cl'|'league', schedule_day=месяц)`.
3. Пересборка `common.db` при необходимости.

**Пришли текстом** голы/передачи по матчам (или один файл `scripts/stats_input_m3_m5.txt`), затем сделаю запись.
